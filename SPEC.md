# 123秒传 App - 产品规格说明书

## 1. 功能范围

本 App 是 123云盘 秒传功能的独立工具，仅包含两个核心功能：

| 功能 | 描述 |
|------|------|
| **生成秒传** | 选择本地文件 → 计算文件Hash → 生成秒传链接 |
| **保存秒传** | 粘贴秒传链接 → 秒传转存到123云盘 |

**不包括**：文件浏览、文件夹管理、分享等 123云盘 其他功能。

---

## 2. 用户交互流程

```
启动 App
    │
    ▼
┌─────────────────┐
│   登录页        │
│  (账号+密码)     │──── 登录成功 ────→ 主页面
│  [登录按钮]     │
└─────────────────┘
        │ 登录失败 → 显示错误提示
        ▼
┌─────────────────────────┐
│      主页面              │
│                          │
│  [生成秒传]   [保存秒传]  │
│                          │
│  ── 选择文件 ──           │
│  [文件选择按钮]           │
│  显示: filename.zip      │
│  显示: 12.5 MB           │
│  显示: 计算中... ████░░  │
│                          │
│  ── 链接输入 ──           │
│  [粘贴秒传链接]           │
│                          │
│  [开始生成]  [开始保存]   │
│                          │
│  ── 结果展示 ──           │
│  秒传链接: 123FLCPV2$... │
│  [复制] [一键复制全部]    │
└─────────────────────────┘
```

---

## 3. 技术方案

### 3.1 前端（Web）
- **单文件 HTML**（index.html），无需后端服务器
- **MD5 计算**：使用 SparkMD5（JS实现，浏览器可用）
- **API 调用**：直接请求 123云盘 公开接口
- **登录态**：Token 存 localStorage（加密）

### 3.2 打包为 Android App
- 使用 **Android Studio** 创建 WebView 项目
- 将 HTML 文件打包进 assets 目录
- App 内打开即用，无需联网（登录需联网）
- 应用名：**123秒传**
- 包名：`com.qianqian123fastlink`

### 3.3 API 映射（来自原始脚本）

| 功能 | API 路径 | 方法 |
|------|----------|------|
| 登录 | `/b/api/user/login_info` | GET |
| 获取文件Hash | `/b/api/file/info` | POST |
| 秒传检测 | `/b/api/file/upload_request` | POST |
| 创建文件夹 | `/b/api/file/upload_request` | POST |
| 获取文件列表 | `/b/api/file/list/new` | GET |

---

## 4. UI 设计

### 4.1 视觉风格
- **风格**：Material Design 3 简约风格
- **主色**：#4CAF50（绿色，与123云盘品牌色呼应）
- **背景**：白色 / 浅灰底
- **圆角卡片**：12px 圆角，轻阴影

### 4.2 页面结构

**登录页**
- LOGO + App 名称
- 账号输入框
- 密码输入框（显示/隐藏切换）
- 登录按钮（loading 状态）
- 底部版权声明

**主页**
- 顶部导航栏（App名称 + 退出登录）
- Tab 切换：生成 | 保存
- 文件选择区（拖拽或点击选择）
- 结果展示区（复制按钮）

---

## 5. 数据安全

- 用户密码**不存储**，仅在登录时使用一次
- 登录成功后存储 `authorToken` 和 `LoginUuid` 到 localStorage
- App 退出登录时清除所有本地数据
- **警告提示**：App 为第三方工具，账号安全由用户自行承担

---

## 6. 错误处理

| 场景 | 处理 |
|------|------|
| 网络错误 | Toast 提示"网络异常，请检查网络" |
| 登录失败 | 显示"账号或密码错误" |
| 文件太大（>4GB） | 提示"文件超过限制" |
| 秒传失败（文件不存在） | 提示"文件秒传失败，可能已被删除" |
| 未登录操作 | 自动跳转登录页 |

---

## 7. 项目文件结构

```
123FastLinkApp/
├── SPEC.md
├── index.html          # 完整 Web 应用（单文件）
├── README.md           # 使用说明
└── android/            # Android Studio 项目
    └── (标准 Android 项目结构)
```
